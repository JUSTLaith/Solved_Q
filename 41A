#include <iostream>
#include <string>

using namespace std;

int main()
{
	string str1,str2;
	cin>>str1>>str2;
	
	bool A=true;
	
	for(int i=0,j=str2.length()-1;i<str1.length()&&j>=0;i++,j--)
		if(str1[i]!=str2[j])
			A=false;
	
	if(A)
		cout<<"YES"<<endl;
	else
		cout<<"NO"<<endl;

    return 0;
}
